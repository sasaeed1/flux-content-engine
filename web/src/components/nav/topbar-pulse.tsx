'use client';

import { EnginePulse } from '@/components/flux/engine-pulse';

/** Compact Engine Pulse mirror for the Topbar (client island). */
export function TopbarPulse() {
  return <EnginePulse />;
}
